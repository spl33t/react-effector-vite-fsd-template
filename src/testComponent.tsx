import React from "react"

const TestComponent = () => {
  return (
    <div>
      <input onChange={() => ({})} onSubmit={() => ({})} onBlur={() => ({})} />
    </div>
  )
}

export default TestComponent
